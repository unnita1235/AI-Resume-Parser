# ============================================
# AI Resume Parser - Backend Environment Variables
# ============================================

# === Server Configuration ===
PORT=5000
NODE_ENV=production

# === Database Configuration ===
# MongoDB Atlas Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database
# Sign up for free tier at: https://www.mongodb.com/cloud/atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/resume-parser

# === File Upload Configuration ===
# Directory to store uploaded files
UPLOAD_PATH=./uploads

# Maximum file size in bytes (default: 10MB = 10485760 bytes)
MAX_FILE_SIZE=10485760

# === Google Gemini API (Optional - for future AI enhancements) ===
# Get API key from: https://ai.google.dev/
GOOGLE_GEMINI_API_KEY=your_gemini_api_key_here

# === CORS Configuration ===
# Comma-separated list of allowed origins (for production)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# === Logging ===
# Log level: error, warn, info, debug
LOG_LEVEL=info

# === Security ===
# JWT Secret for authentication (if implementing auth in future)
JWT_SECRET=your_super_secret_jwt_key_change_this

# === Deployment ===
# Set to 'true' to enable health check endpoint (recommended for Render/Railway)
HEALTH_CHECK_ENABLED=true

# ============================================
# SETUP INSTRUCTIONS
# ============================================

# 1. MongoDB Atlas (Free Tier):
#    - Go to https://www.mongodb.com/cloud/atlas
#    - Create a free account
#    - Create a cluster (M0 free tier)
#    - Create a database user with username and password
#    - Get connection string from "Connect" button
#    - Add your IP address to IP Whitelist (or 0.0.0.0 for testing)
#    - Replace MONGODB_URI with your connection string

# 2. Local Development:
#    - Copy this file to .env (in backend/ directory)
#    - Update MONGODB_URI with your actual MongoDB connection string
#    - Run: npm install
#    - Run: npm run dev

# 3. Production Deployment (Render):
#    - Keep PORT as-is (Render sets it automatically)
#    - Add MONGODB_URI in Render environment variables
#    - Set NODE_ENV=production
#    - Ensure CORS_ORIGINS is set to your frontend domain

# 4. Production Deployment (Railway):
#    - Similar to Render
#    - Railway sets PORT automatically
#    - Add environment variables in Railway dashboard
