# =================================================================
# Backend Environment Variables (.env)
# =================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# =================================================================

# -----------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------
PORT=5000
NODE_ENV=development

# -----------------------------------------------------------------
# REQUIRED: Google Gemini API Key for AI-powered resume parsing
# Get your free API key: https://aistudio.google.com/app/apikey
# -----------------------------------------------------------------
GOOGLE_GEMINI_API_KEY=your_google_gemini_api_key_here

# -----------------------------------------------------------------
# MongoDB Connection String (OPTIONAL but recommended)
# Get free tier: https://cloud.mongodb.com
# Example: mongodb+srv://username:password@cluster.mongodb.net/resumes
# -----------------------------------------------------------------
MONGODB_URI=

# -----------------------------------------------------------------
# CORS Whitelist - CRITICAL for frontend-backend connectivity
# -----------------------------------------------------------------
# Comma-separated list of allowed origins (NO trailing slashes!)
# Include BOTH localhost for development AND production URLs
#
# For local development:
CORS_WHITELIST=http://localhost:3000,http://localhost:3001,https://ai-resume-parser-seven.vercel.app

# For production only (uncomment and customize):
# CORS_WHITELIST=https://your-frontend.vercel.app,https://www.yourdomain.com

# -----------------------------------------------------------------
# File Upload Configuration
# -----------------------------------------------------------------
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# -----------------------------------------------------------------
# Demo Mode
# Set to true to bypass API requirements for testing
# -----------------------------------------------------------------
DEMO_MODE=false

# -----------------------------------------------------------------
# Keep-Alive for Free Tier Hosting (Render, Railway, etc.)
# Ping interval in milliseconds (25000 = 25 seconds)
# This prevents the service from sleeping due to inactivity
# -----------------------------------------------------------------
KEEP_ALIVE_INTERVAL=25000

# -----------------------------------------------------------------
# Deployment Instructions
# -----------------------------------------------------------------
# Render:
#   1. Go to your Web Service in Render dashboard
#   2. Navigate to "Environment" tab
#   3. Add all required environment variables above
#   4. IMPORTANT: Add your Vercel frontend URL to CORS_WHITELIST
#   5. Deploy or redeploy the service
#
# Railway:
#   1. Go to your project settings
#   2. Navigate to "Variables" tab
#   3. Add all required environment variables
#   4. Railway will automatically redeploy
#
# Docker:
#   Pass environment variables via docker run:
#   docker run -e PORT=5000 -e GOOGLE_GEMINI_API_KEY=xxx ...
# -----------------------------------------------------------------
