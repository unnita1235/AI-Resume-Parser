# Frontend Environment Variables

# Google Gemini API Key (REQUIRED for AI features)
# Get your free API key: https://aistudio.google.com/app/api-keys
GOOGLE_GENAI_API_KEY=your_google_gemini_api_key_here

# Google AI API Key (Alternative name for backend compatibility)
GOOGLE_AI_API_KEY=your_google_ai_api_key_here

# Backend API URL (Required for frontend)
# For local development: http://localhost:5000
# For production: your deployed backend URL (e.g., https://your-backend.onrender.com)
NEXT_PUBLIC_API_URL=http://localhost:5000

# MongoDB Connection (Optional - app works without it but data doesn't persist)
# Get a free MongoDB Atlas cluster: https://www.mongodb.com/cloud/atlas
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Node Environment
NODE_ENV=development

# Optional: CORS Whitelist for production (comma-separated URLs)
# CORS_WHITELIST=https://your-frontend.vercel.app,https://your-other-domain.com

# Optional: Demo Mode (set to 'true' to enable demo mode when backend is unavailable)
# DEMO_MODE=false

# Optional: Max file upload size in bytes (default: 10MB)
# MAX_FILE_SIZE=10485760
