# =================================================================
# Frontend Environment Variables (.env.local)
# =================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# IMPORTANT: Never commit .env.local to version control!
# =================================================================

# -----------------------------------------------------------------
# REQUIRED: Google Gemini API Key for AI features
# Get your free API key: https://aistudio.google.com/app/apikey
# -----------------------------------------------------------------
GOOGLE_GENAI_API_KEY=your_google_gemini_api_key_here

# -----------------------------------------------------------------
# Backend API URL - CRITICAL for frontend-backend connectivity
# -----------------------------------------------------------------
# The NEXT_PUBLIC_ prefix is REQUIRED to expose this variable
# to the browser (client-side JavaScript).
#
# For local development (backend running on port 5000):
NEXT_PUBLIC_API_URL=http://localhost:5000

# For production, use your deployed backend URL (uncomment):
# NEXT_PUBLIC_API_URL=https://your-backend.onrender.com

# -----------------------------------------------------------------
# Deployment Instructions
# -----------------------------------------------------------------
# Vercel:
#   1. Go to your project settings in Vercel dashboard
#   2. Navigate to "Environment Variables"
#   3. Add GOOGLE_GENAI_API_KEY with your API key
#   4. Add NEXT_PUBLIC_API_URL with your production backend URL
#   5. Redeploy your application
#
# Netlify:
#   1. Go to Site Settings > Build & Deploy > Environment
#   2. Add both variables as shown above
#   3. Trigger a new deployment
# -----------------------------------------------------------------
